name: Example REST API
slug: example-api
db:
  type: mongo
schema:
  $schema: "http://json-schema.org/draft-07/schema#"
  properties:
    name:
      type: string
    color:
      type: string
  required:
    - name
    - color
