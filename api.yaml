name: Cars
slug: cars
schema:
  $schema: "http://json-schema.org/draft-07/schema#"
  properties:
    name:
      type: string
    color:
      type: string
    year: 
      type: integer
  required:
    - name
    - color
    - year